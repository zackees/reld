# reld-layout-schema

This crate provides data structures for representing and persisting to disk, the layout used by a
linker such as [Reld](https://github.com/zackees/reld). This layout information can then be
used by tools such as reld-diff.
